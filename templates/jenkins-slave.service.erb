[Unit]
Description=Jenkins Slave Swarm Client
Requires=network-online.target
After=network-online.target

[Service]
Type=simple
ExecStart=<%= @slave_home %>/start-slave.sh
Restart=always
RestartSec=60
StartLimitInterval=0
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
